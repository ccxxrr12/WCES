## ESP-IDF Managed Component Dependencies (ADR-045)
dependencies:
  ## LVGL graphics library
  lvgl/lvgl: "~8.3"

  ## CSI gain lock for stable amplitude (disables AGC for sensing)
  espressif/esp_csi_gain_ctrl: "^0.1.5"
